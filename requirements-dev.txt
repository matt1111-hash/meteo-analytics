# =============================================================================
# requirements-dev.txt - Dev/test dependencies
# pip install -r requirements-dev.txt
# =============================================================================

# Runtime (install first)
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=1.4.0
pytest-cov>=4.1.0
pytest-qt>=4.5.0
pytest-timeout>=2.4.0
mutmut>=2.4

# Linting & Formatting
# Pinned to match requirements.lock so health-check (which installs from these
# files) lints with the same versions as CI (which installs the lock). A newer
# ruff enforces PLR0917 on pre-existing code the project never opted into.
ruff==0.15.10
mypy==1.17.0

# Complexity & Metrics
radon>=5.0,<7
xenon>=0.9
wily>=1.25

# Architecture
import-linter>=2.0

# Code Quality
vulture>=2.11
bandit>=1.7.7
detect-secrets>=1.4
pip-audit>=2.6

# Dev Tools
pre-commit>=3.7
types-requests>=2.33.0.20260518
