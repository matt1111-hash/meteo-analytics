# Global Weather Analyzer - Unified Dependencies with Folium Interactive Maps
# Install: pip install -r requirements.txt
# Refactored from legacy PyQt5 monolithic code to modern PySide6 modular architecture
# ✅ UPDATED: Full Folium interactive map support added

# =============================================================================
# CORE RUNTIME DEPENDENCIES
# =============================================================================

# IMPORTANT Linux Setup:
# export LD_LIBRARY_PATH="$VIRTUAL_ENV/lib/python3.12/site-packages/PySide6/Qt/lib:$LD_LIBRARY_PATH"

# Core GUI Framework (PyQt5 -> PySide6 migration)
PySide6>=6.6.0

# Data Processing & Analysis
pandas>=2.0.0
numpy>=1.24.0

# Networking & API Calls (unified sync/async approach)
httpx[http2]>=0.25.0

# Visualization & Charts
matplotlib>=3.7.0

# Web Scraping (using built-in parser, no external C dependencies)
beautifulsoup4>=4.12.0

# 🗺️ INTERACTIVE MAPS - FOLIUM INTEGRATION (MAJOR FEATURE)
folium>=0.14.0          # Interactive Leaflet.js maps with county click support
branca>=0.6.0          # Folium color mapping and styling support

# Interactive Features (optional but valuable)
plotly>=5.17.0          # Interactive charts and advanced visualizations
schedule>=1.2.0         # Automated data refresh and background tasks

# Type Hints Support
typing-extensions>=4.5.0

# =============================================================================
# GEOSPATIAL DEPENDENCIES - FOLIUM MAP SUPPORT
# =============================================================================

# 🌍 GEOSPATIAL DATA PROCESSING (REQUIRED FOR FOLIUM MAPS)
geopandas>=0.14.0       # Geographic data processing for county boundaries
shapely>=2.0.0          # Geometric operations for map features
fiona>=1.9.0           # Geospatial file I/O (GeoJSON, Shapefile support)
pyproj>=3.6.0          # Coordinate system transformations

# =============================================================================
# ANALYTICS DEPENDENCIES (FÁZIS 5)
# =============================================================================

# Statistical Analysis & Machine Learning
scipy>=1.9.0            # Statistical operations (Z-score, percentiles)
scikit-learn>=1.3.0     # Anomaly detection algorithms (updated version)

# =============================================================================
# FUTURE DEVELOPMENT DEPENDENCIES (FÁZIS 6-10)
# =============================================================================

# Geographic & Location Services (FÁZIS 9)
geopy>=2.3.0            # Geocoding and distance calculations

# Advanced API & Caching (FÁZIS 10)
requests-cache>=1.1.0   # Advanced API caching system

# Export & Reporting (FÁZIS 8)
reportlab>=3.6.0        # PDF report generation
openpyxl>=3.0.0         # Excel export support

# Performance & Scalability (FÁZIS 10) - OPTIONAL
redis>=4.3.0            # Redis caching (optional, for production)

# =============================================================================
# DEVELOPMENT DEPENDENCIES (OPTIONAL)
# =============================================================================

# Testing Framework
pytest>=7.4.0
pytest-asyncio>=0.21.0      # For testing async httpx calls
pytest-qt>=4.2.0           # For testing PySide6 widgets
pytest-cov>=4.1.0          # Coverage reporting

# Code Formatting & Linting
black>=23.9.0               # Code formatter
ruff>=0.1.0                 # Fast Python linter (replaces flake8, isort)

# Type Checking
mypy>=1.6.0
types-requests>=2.31.0      # Type stubs for better static analysis

# Development Tools
pre-commit>=3.5.0           # Git hooks for automated checks
ipython>=8.16.0             # Enhanced REPL for debugging

# =============================================================================
# FOLIUM INTERACTIVE MAP FEATURES
# =============================================================================

# 🗺️ CORE FOLIUM FEATURES ENABLED:
# ✅ Interactive Leaflet.js maps with smooth zoom/pan
# ✅ Clickable Hungarian counties with JavaScript bridge
# ✅ Hover tooltips with county information
# ✅ Weather overlay heatmaps (temperature, precipitation)
# ✅ Multiple map styles (OpenStreetMap, CartoDB light/dark, Stamen)
# ✅ Bidirectional synchronization (location selector ↔ map)
# ✅ HTML export with full interactivity preserved
# ✅ Theme support (light/dark map styles)
# ✅ Custom markers and county highlighting
# ✅ MiniMap, fullscreen, measurement tools
# ✅ Mouse position coordinates display

# 🌍 GEOSPATIAL DATA REQUIREMENTS:
# - Hungarian county boundaries (GeoJSON format)
# - Climate region definitions
# - Coordinate system transformations (EPSG:4326)
# - Geometry operations (bounds calculation, centroid finding)

# =============================================================================
# INSTALLATION INSTRUCTIONS
# =============================================================================

# 🚀 PRODUCTION INSTALL (runtime + analytics + Folium maps):
# pip install PySide6>=6.6.0 pandas>=2.0.0 numpy>=1.24.0 httpx[http2]>=0.25.0 matplotlib>=3.7.0 beautifulsoup4>=4.12.0 folium>=0.14.0 branca>=0.6.0 geopandas>=0.14.0 shapely>=2.0.0 fiona>=1.9.0 pyproj>=3.6.0 plotly>=5.17.0 schedule>=1.2.0 typing-extensions>=4.5.0 scipy>=1.9.0 scikit-learn>=1.3.0

# 🗺️ FOLIUM MAPS ONLY (minimum for interactive map features):
# pip install folium>=0.14.0 branca>=0.6.0 geopandas>=0.14.0 shapely>=2.0.0 fiona>=1.9.0 pyproj>=3.6.0

# 🔬 DEVELOPMENT INSTALL (all dependencies):
# pip install -r requirements.txt

# 📊 ANALYTICS ONLY (minimum for FÁZIS 5):
# pip install scipy>=1.9.0 scikit-learn>=1.3.0

# 🔮 FUTURE FEATURES (FÁZIS 6-10):
# pip install geopy>=2.3.0 requests-cache>=1.1.0 reportlab>=3.6.0 openpyxl>=3.0.0 redis>=4.3.0

# =============================================================================
# FOLIUM INSTALLATION TROUBLESHOOTING
# =============================================================================

# 🐧 LINUX USERS:
# If you encounter issues with Fiona/GDAL dependencies:
# sudo apt-get install gdal-bin libgdal-dev
# pip install GDAL==$(gdal-config --version)

# 🪟 WINDOWS USERS:
# If GeoPandas installation fails:
# conda install geopandas (recommended)
# OR use pre-compiled wheels: pip install --find-links https://girder.github.io/large_image_wheels GDAL Fiona

# 🍎 MACOS USERS:
# brew install gdal
# pip install GDAL==$(gdal-config --version)

# =============================================================================
# FEATURE COMPATIBILITY MATRIX
# =============================================================================

# ✅ CORE FEATURES (always work):
# - Weather data analysis and charts
# - Location selection (dropdown menus)
# - Analytics dashboard with statistics
# - Data export and reports

# 🗺️ FOLIUM FEATURES (require geospatial dependencies):
# - Interactive Leaflet.js maps
# - Clickable county boundaries
# - Weather overlay heatmaps
# - HTML map export

# 🔄 GRACEFUL DEGRADATION:
# If Folium/GeoPandas not available:
# - Falls back to static HTML map
# - All other features remain functional
# - Installation message displayed to user

# =============================================================================
# VERSION COMPATIBILITY
# =============================================================================

# 🐍 PYTHON VERSION: 3.9+ (required for PySide6 and modern type hints)
# 🖥️ OPERATING SYSTEMS: Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
# 💾 MEMORY: 4GB+ recommended (geospatial processing can be memory intensive)
# 🗺️ BROWSER: Any modern browser for Folium HTML export viewing

# =============================================================================
# PERFORMANCE NOTES
# =============================================================================

# 🚀 STARTUP TIME:
# - First run: ~5-10 seconds (GeoPandas loading + GeoJSON parsing)
# - Subsequent runs: ~2-3 seconds (cached data)

# 💾 MEMORY USAGE:
# - Base application: ~50MB
# - With GeoPandas + county data: ~150MB
# - Folium map generation: ~50MB additional

# 🌐 NETWORK:
# - Weather API calls: minimal bandwidth
# - Folium map tiles: ~1MB per map style
# - Works fully offline after initial setup